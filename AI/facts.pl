
    % facts.pl
    % Define some simple facts
    human(socrates).
    human(plato).
    philosopher(socrates).
    philosopher(plato).

    % Queries you can run:
    % ?- human(socrates).
    % ?- philosopher(plato).
    